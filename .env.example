# Optional. Address other machines reach this host on, in addition to 127.0.0.1.
# Unset: the board is reachable from this machine only. 0.0.0.0 publishes on every
# interface; a specific address (a VPN or tailnet IP, say) publishes on that one.
BOT_BOARD_BIND_IP=0.0.0.0
